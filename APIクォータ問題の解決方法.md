# APIクォータ問題の解決方法

## 現在の問題

サーバーログから、**OpenAI APIのクォータが上限に達している**ことが判明しました。

エラーメッセージ：
```
OpenAI APIエラー: 429
insufficient_quota: You exceeded your current quota
```

## 解決方法

### 方法1: OpenAIアカウントでクレジットを追加

1. **OpenAI Platformにログイン**
   - https://platform.openai.com/ にアクセス

2. **Billing（請求）セクションを開く**
   - 左側のメニューから「Billing」を選択

3. **クレジットを追加**
   - 「Add payment method」または「Add credits」をクリック
   - クレジットカード情報を入力してクレジットを追加

### 方法2: プランをアップグレード

1. **Usage & Billing**を確認
   - 現在のプランと使用量を確認

2. **プランを変更**
   - より高いプランにアップグレードすることで、クォータが増えます

### 方法3: 新しいAPIキーを取得

1. **API Keys**セクションを開く
   - 左側のメニューから「API keys」を選択

2. **新しいAPIキーを作成**
   - 「Create new secret key」をクリック
   - 新しいAPIキーをコピー

3. **`.env.local`ファイルを更新**
   - 新しいAPIキーを設定

## 現在の状態

- **コードは正しく動作しています**
  - 会話履歴は正しく送信されている
  - メッセージ配列も正しく構築されている
  - 最後の発言への応答を求めるuserメッセージも正しく追加されている

- **問題はAPIクォータのみ**
  - クォータを追加すれば、正常に動作します

## 一時的な対策

クォータが追加されるまでの間、モックデータを改善しました：
- 会話履歴の最後の発言を参照するように修正
- 最後の発言を引用して応答を生成
- より多様な応答パターンを追加

これにより、APIが使えない場合でも、会話履歴を参照した応答が生成されます。

## 確認事項

クレジットを追加した後：
1. サーバーを再起動
2. 新しいディベートを開始
3. サーバーログで429エラーが出ていないか確認
4. `✅✅✅ OpenAI API成功！` というログが表示されることを確認

