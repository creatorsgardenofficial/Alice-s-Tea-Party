# サーバー起動確認方法

## 確認手順

### ステップ1: コマンドプロンプトを開く

Windowsキーを押して「cmd」と入力し、コマンドプロンプトを開く

### ステップ2: プロジェクトフォルダに移動

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
```

### ステップ3: サーバーの状態確認

#### 方法A: ポートの使用状況を確認
```cmd
netstat -ano | findstr :3000
netstat -ano | findstr :3002
```

何か表示されれば、そのポートでプロセスが動作しています。

#### 方法B: Node.jsプロセスを確認
```cmd
tasklist | findstr node
```

Node.jsプロセスが表示されれば、サーバーが起動している可能性があります。

### ステップ4: サーバーを起動/再起動

**既に起動している場合:**
1. コマンドプロンプトで実行中のプロセスを確認
2. 別のコマンドプロンプトウィンドウを開いて以下を実行：

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
npm run dev
```

**起動していない場合:**
```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
npm run dev
```

### ステップ5: 正常起動の確認

正常に起動すると、以下のようなメッセージが表示されます：

```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
- Ready in 2.5s
```

または

```
▲ Next.js 14.x.x
- Local:        http://localhost:3002
- Ready in 2.5s
```

### ステップ6: ブラウザでアクセス

表示されたURL（`http://localhost:3000` または `http://localhost:3002`）にアクセス

## トラブルシューティング

### 問題: ポートが既に使用されている

**エラーメッセージ例:**
```
Error: listen EADDRINUSE: address already in use :::3000
```

**解決方法:**
1. 使用しているポートを確認：
   ```cmd
   netstat -ano | findstr :3000
   ```
2. プロセスIDを確認して、そのプロセスを終了：
   ```cmd
   taskkill /PID <プロセスID> /F
   ```
3. または、別のポートを使用：
   ```cmd
   npm run dev -- -p 3003
   ```

### 問題: 依存関係がインストールされていない

**エラーメッセージ例:**
```
Cannot find module 'next'
```

**解決方法:**
```cmd
npm install
```

### 問題: ビルドエラー

**確認方法:**
```cmd
npm run build
```

エラーメッセージがあれば、その内容を確認してください。

## 次のステップ

上記の確認を行い、結果を共有してください。特に：
- サーバー起動時のメッセージ
- エラーメッセージ（あれば）
- ブラウザでの表示状況
