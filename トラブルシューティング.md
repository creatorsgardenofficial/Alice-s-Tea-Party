# ローディング中の問題 - トラブルシューティング

## 問題: http://localhost:3003 がローディング中

### 確認手順

#### ステップ1: サーバーが起動しているか確認

1. コマンドプロンプトで以下を確認：
```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
npm run dev
```

2. 以下のメッセージが表示されているか確認：
```
▲ Next.js 14.x.x
- Local:        http://localhost:3003
- Ready in X.Xs
```

**もし「Ready」が表示されていなければ、サーバーは起動していません。**

#### ステップ2: シンプルなページで確認

以下のURLにアクセスしてください：
```
http://localhost:3003/simple
```

- **表示される場合**: サーバーは動作しています。メインページの問題です。
- **表示されない場合**: サーバーが起動していないか、別のポートで起動しています。

#### ステップ3: テストページで確認

以下のURLにアクセスしてください：
```
http://localhost:3003/test
```

#### ステップ4: ブラウザの開発者ツールで確認

1. ブラウザで `F12` を押す
2. 「Console」タブを開く
3. エラーメッセージを確認

**よくあるエラー:**
- `Failed to fetch` → サーバーが起動していない
- `Network Error` → サーバーに接続できない
- フォント関連のエラー → フォント読み込みの問題

#### ステップ5: ポートの確認

別のポートで起動している可能性があります。確認：

```cmd
netstat -ano | findstr :3000
netstat -ano | findstr :3002
netstat -ano | findstr :3003
```

## 解決方法

### 方法1: サーバーを再起動

1. 実行中のサーバーを停止（`Ctrl+C`）
2. 以下を実行：
```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
npm run dev
```

### 方法2: 別のポートで起動

```cmd
npm run dev -- -p 3004
```

その場合、URLは `http://localhost:3004` になります。

### 方法3: キャッシュをクリア

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
rmdir /s /q .next
npm run dev
```

### 方法4: 依存関係を再インストール

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
rmdir /s /q node_modules
npm install
npm run dev
```

## よくある原因

1. **サーバーが起動していない**
   - 解決: `npm run dev` を実行

2. **ビルドエラー**
   - 確認: コマンドプロンプトにエラーメッセージがないか
   - 解決: エラーメッセージに従って修正

3. **フォント読み込みが遅い**
   - 確認: 少し待つ（10-20秒）
   - 解決: `/simple` ページで確認

4. **ポートが既に使用中**
   - 確認: `netstat -ano | findstr :3003`
   - 解決: 別のポートを使用

## 次のステップ

上記の確認を行い、結果を共有してください：
- `/simple` ページが表示されるか
- コマンドプロンプトのメッセージ
- ブラウザのコンソールエラー（あれば）
