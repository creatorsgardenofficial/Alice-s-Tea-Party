# 緊急：サーバー起動手順

## 現在の状況
- `/simple` ページも表示されない
- コンソールログも何も出ていない
- **サーバーが起動していない可能性が高い**

## すぐに試す手順

### 方法1: コマンドプロンプトから直接起動（最も確実）

1. **Windowsキーを押して「cmd」と入力**
2. **コマンドプロンプトを開く**
3. **以下を実行：**

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
npm run dev
```

4. **以下のようなメッセージが表示されるまで待つ：**
```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
- Ready in 2.5s
```

5. **表示されたら、ブラウザでそのURLにアクセス**

### 方法2: 別のポートで起動

ポート3003で問題がある可能性があります：

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
npm run dev -- -p 3000
```

その後、`http://localhost:3000` にアクセス

### 方法3: .nextフォルダを削除して再起動

ビルドキャッシュの問題の可能性：

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
rmdir /s /q .next
npm run dev
```

## 確認ポイント

### 1. 依存関係がインストールされているか
```cmd
dir node_modules
```

フォルダが存在すればOK

### 2. Node.jsがインストールされているか
```cmd
node --version
npm --version
```

バージョンが表示されればOK

### 3. ポートが使用中か
```cmd
netstat -ano | findstr :3000
netstat -ano | findstr :3003
```

何も表示されなければ、ポートは空いています

## よくある問題と解決方法

### 問題1: 「npm は認識されていません」
→ Node.jsをインストール：https://nodejs.org/

### 問題2: 「Cannot find module 'next'」
→ 依存関係をインストール：
```cmd
npm install
```

### 問題3: ポートが使用中
→ 別のポートを使用：
```cmd
npm run dev -- -p 3004
```

### 問題4: ビルドエラー
→ エラーメッセージを確認して共有してください

## 次のステップ

上記の方法1を試してください。それでもダメな場合：

1. コマンドプロンプトに表示されたエラーメッセージを共有
2. `npm --version` と `node --version` の結果を共有
3. `dir node_modules` の結果を共有

これらを共有していただければ、より具体的な解決策を提案できます。
