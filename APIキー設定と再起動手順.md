# APIキー設定と再起動手順（緊急対応）

## 現在の状態

サーバーログから、**APIキーが未設定**のためモックデータが使用されています：

```
[API] APIキーの確認: ❌ 未設定 - モックデータを使用
```

## 解決方法

### ステップ1: `.env.local`ファイルを作成

以下のいずれかの方法で`.env.local`ファイルを作成してください。

#### 方法A: バッチファイルを使用（推奨）

1. **`APIキー設定.bat`** をダブルクリックして実行
2. プロンプトが表示されたら `Y` を入力
3. ファイルが作成されたら、サーバー再起動を選択

#### 方法B: 手動で作成

1. **エクスプローラーでプロジェクトフォルダを開く**
   ```
   C:\Users\yamada\Documents\アリスのお茶会
   ```

2. **新しいテキストファイルを作成**
   - 右クリック → 新規作成 → テキスト文書

3. **ファイル名を変更**
   - ファイル名を `.env.local` に変更（**拡張子なし**）
   - 「拡張子を変更すると、ファイルが使えなくなる可能性があります」という警告が表示されたら「はい」を選択

4. **ファイルを開いて以下の内容を貼り付け**
   ```
   OPENAI_API_KEY=your_api_key_here
   ```

5. **保存して閉じる**

### ステップ2: サーバーを再起動（重要！）

**⚠️ 重要：`.env.local`ファイルを変更した場合、必ずサーバーを再起動してください！**

環境変数はサーバー起動時に読み込まれます。

#### 再起動方法

1. **実行中のサーバーを停止**
   - サーバーが実行中のコマンドプロンプトで `Ctrl+C` を押す

2. **サーバーを再起動**
   - 方法1: `完全クリーンアップ.bat` をダブルクリック
   - 方法2: `サーバー再起動.bat` をダブルクリック
   - 方法3: コマンドプロンプトで `npm run dev` を実行

### ステップ3: 設定が反映されたか確認

ディベートを開始して、サーバーのログを確認してください。

#### ✅ 成功している場合：

```
[API] ==========================================
[API] APIキーの確認: ✅ 設定済み (sk-proj-EM...)
[API] ==========================================
[API] OpenAI APIを呼び出し中...
[API] ✅✅✅ OpenAI API成功！ ✅✅✅
```

#### ❌ まだ失敗している場合：

```
[API] APIキーの確認: ❌ 未設定 - モックデータを使用
```

→ ステップ1に戻って、`.env.local`ファイルが正しく作成されているか確認してください。

## よくある問題

### 問題1: ファイル名が `.env.local.txt` になっている

- **解決方法**: ファイル名を `.env.local`（拡張子なし）に変更
- エクスプローラーで「表示」→「ファイル名拡張子」を有効にして確認

### 問題2: サーバーを再起動しても反映されない

- **解決方法**: 
  1. `.next` フォルダを削除
  2. `完全クリーンアップ.bat` を実行
  3. サーバーを再起動

### 問題3: `.env.local`ファイルが見えない

- **解決方法**: エクスプローラーで「表示」→「隠しファイル」を有効にする
- `.env.local` は隠しファイルとして扱われる場合があります

## 確認チェックリスト

- [ ] `.env.local`ファイルが存在する
- [ ] ファイル名が `.env.local`（拡張子なし）になっている
- [ ] ファイルに `OPENAI_API_KEY=sk-proj-EM...` が記載されている
- [ ] サーバーを再起動した
- [ ] サーバーログに「✅ 設定済み」が表示される
- [ ] API呼び出しが成功している（「✅✅✅ OpenAI API成功！」が表示される）

## 次のステップ

APIキーが正しく設定されれば：
1. 実際のOpenAI APIが呼び出されます
2. 会話がより多様で自然になります
3. 会話履歴を参照した応答が生成されます

もしAPIクォータの問題が発生した場合は、`APIクォータ問題の解決方法.md` を参照してください。

