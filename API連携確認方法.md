# API連携確認方法

## 問題：会話がワンパターン

APIが正しく連携されていない可能性があります。

## 確認方法

### ステップ1: サーバーのログを確認

サーバーを起動したコマンドプロンプトで、ディベートを開始した時に以下を確認：

#### ✅ APIが使われている場合：

```
[API] ==========================================
[API] APIキーの確認: ✅ 設定済み (sk-proj-EM...)
[API] ==========================================
[API] OpenAI APIを呼び出し中...
[API] ✅ OpenAI API成功！
[API] 応答内容: （実際のAPI応答が表示される）
[API] 応答の長さ: 85 文字
```

#### ❌ モックデータが使われている場合：

```
[API] ==========================================
[API] APIキーの確認: ❌ 未設定 - モックデータを使用
[API] ==========================================
[API] モックデータを使用
```

### ステップ2: .env.localファイルの確認

1. エクスプローラーで以下を開く：
   ```
   C:\Users\yamada\Documents\アリスのお茶会\.env.local
   ```

2. ファイル内容を確認：
   ```
   OPENAI_API_KEY=your_api_key_here
   ```

3. **重要：ファイル名が `.env.local` になっているか確認**
   - `.env.local.txt` になっていないか
   - 拡張子なしである必要があります

### ステップ3: サーバーを再起動

**.env.localファイルを変更した場合、必ずサーバーを再起動してください！**

1. 実行中のサーバーを停止（Ctrl+C）
2. `サーバー再起動.bat` を実行
3. または `完全クリーンアップ.bat` を実行

### ステップ4: 実際にディベートを実行して確認

1. ブラウザでアプリにアクセス
2. ディベートを開始
3. **サーバーのログを確認**

## 改善内容

今回の更新で以下を改善しました：

1. **プロンプトの改善**
   - より詳細な指示を追加
   - 多様な応答を促すよう改善
   - 会話の流れを考慮

2. **会話履歴の改善**
   - 直近の会話のみを送信（効率化）
   - より明確なコンテキストを構築

3. **デバッグログの強化**
   - APIキーの状態を明確に表示
   - API成功時に応答内容を表示
   - モックデータ使用時も明確に表示

## トラブルシューティング

### 問題1: 「未設定 - モックデータを使用」と表示される

**原因：** `.env.local` ファイルが正しく読み込まれていない

**解決方法：**
1. `.env.local` ファイルが正しい場所にあるか確認
2. ファイル名が `.env.local`（拡張子なし）になっているか確認
3. ファイル内容に余分なスペースや改行がないか確認
4. **サーバーを再起動**

### 問題2: APIエラーが表示される

**確認項目：**
- APIキーが有効か
- インターネット接続があるか
- OpenAI APIの使用制限に達していないか

**ログ例：**
```
[API] OpenAI APIエラー: 401 {...}
```

→ APIキーが無効の可能性

### 問題3: API成功と表示されるが会話がワンパターン

**改善済み：**
- プロンプトを改善しました
- 会話履歴を考慮するようにしました

**確認：**
- サーバーを再起動して新しいコードが反映されているか確認
- ブラウザのキャッシュをクリア

## 重要

⚠️ **環境変数（`.env.local`）を変更した場合、必ずサーバーを再起動してください！**

⚠️ **`.env.local` ファイルはプロジェクトのルートフォルダに作成してください**
