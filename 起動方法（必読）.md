# ⚠️ 重要：正しい起動方法

## 現在の問題
ターミナルツールではエラーが発生していますが、これはCursorのツールの問題です。
**手動でコマンドプロンプトから起動する必要があります。**

## 正しい起動手順

### 方法1: バッチファイルを使用（最も簡単・推奨）

1. **エクスプローラーで以下のフォルダを開く：**
   ```
   C:\Users\yamada\Documents\アリスのお茶会
   ```

2. **以下のいずれかをダブルクリック：**
   - `起動確認.bat` （確認付きで起動）
   - `start-server.bat` （直接起動）

3. **コマンドプロンプトウィンドウが開き、以下のようなメッセージが表示されるまで待つ：**
   ```
   ▲ Next.js 14.x.x
   - Local:        http://localhost:3000
   - Ready in X.Xs
   ```

4. **表示されたURL（例：http://localhost:3000）にブラウザでアクセス**

### 方法2: コマンドプロンプトから手動実行

1. **Windowsキーを押して「cmd」と入力**
2. **コマンドプロンプトを開く**
3. **以下を順番に実行：**

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
npm run dev
```

4. **「Ready」というメッセージが表示されるまで待つ**
5. **表示されたURLにブラウザでアクセス**

## 確認ポイント

### ✅ サーバーが正常に起動している場合：

コマンドプロンプトに以下のように表示されます：
```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
- Ready in 2.5s

 ✓ Ready in 2.5s
 ○ Compiling / ...
 ✓ Compiled / in 234ms
```

### ❌ エラーが発生している場合：

エラーメッセージが表示されます。以下を確認：
- `Error: Cannot find module` → 依存関係の問題
- `Port XXXX is already in use` → ポートが使用中
- `Failed to compile` → コードにエラー

## トラブルシューティング

### 問題1: 「npm は認識されていません」

**解決方法：**
1. Node.jsをインストール：https://nodejs.org/
2. インストール後、コマンドプロンプトを再起動

### 問題2: ポートが使用中

**解決方法：**
別のポートで起動：
```cmd
npm run dev -- -p 3005
```
その後、`http://localhost:3005` にアクセス

### 問題3: ビルドエラー

**解決方法：**
```cmd
rmdir /s /q .next
npm run dev
```

## 最も簡単な確認方法

**`起動確認.bat` をダブルクリック**するだけです。
このファイルは、すべての確認を行い、自動的にサーバーを起動します。
