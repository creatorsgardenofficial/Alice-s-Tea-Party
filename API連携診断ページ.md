# API連携診断方法

## 問題：会話がワンパターンでテンプレート的

## 診断手順

### ステップ1: サーバーのログを確認（最重要）

サーバーを起動したコマンドプロンプトで、ディベートを開始した時に以下を確認：

#### ✅ APIが正しく使われている場合：

```
[API] ==========================================
[API] APIキーの確認: ✅ 設定済み (sk-proj-EM...)
[API] ==========================================
[API] OpenAI APIを呼び出し中...
[API] ✅✅✅ OpenAI API成功！ ✅✅✅
[API] 完全な応答: （実際のAPI応答の全文が表示される）
[API] 応答の長さ: 85 文字
[API] ==========================================
```

#### ❌ モックデータが使われている場合：

```
[API] ==========================================
[API] APIキーの確認: ❌ 未設定 - モックデータを使用
[API] ==========================================
[API] ⚠️⚠️⚠️ モックデータを使用 ⚠️⚠️⚠️
[API] 注意：実際のChatGPT APIは呼び出されていません！
[API] .env.localファイルを確認してください。
```

### ステップ2: ブラウザのコンソールで確認

1. ブラウザで F12 を押して開発者ツールを開く
2. 「Console」タブを選択
3. ディベートを開始
4. 以下を確認：

#### ✅ APIが使われている場合：
```
✅✅✅ APIが使用されています！応答: （応答の最初の50文字）
```

#### ❌ モックデータが使われている場合：
```
⚠️⚠️⚠️ モックデータが使用されています！応答: （応答の最初の50文字）
```

### ステップ3: .env.localファイルの確認

1. エクスプローラーで以下を開く：
   ```
   C:\Users\yamada\Documents\アリスのお茶会\.env.local
   ```

2. ファイルが存在するか確認

3. 内容が以下になっているか確認：
   ```
   OPENAI_API_KEY=your_api_key_here
   ```

4. **重要：ファイル名が `.env.local`（拡張子なし）になっているか確認**
   - `.env.local.txt` になっていないか
   - ファイルのプロパティで拡張子を確認

### ステップ4: サーバーを再起動

**.env.localファイルを変更した場合、必ずサーバーを再起動してください！**

1. 実行中のサーバーを停止（Ctrl+C）
2. `サーバー再起動.bat` を実行
3. または `完全クリーンアップ.bat` を実行

## 改善内容（今回の更新）

1. **API連携の明確な表示**
   - ✅✅✅ や ⚠️⚠️⚠️ で明確に表示
   - クライアント側でも確認可能

2. **プロンプトの改善**
   - 「テンプレート的な表現を避ける」と明示
   - 多様な表現技法を促す指示を追加
   - 同じ接続詞の連続使用を禁止

3. **Temperatureとペナルティの調整**
   - temperature: 1.2（より創造的）
   - presence_penalty: 0.6（トピックの繰り返し防止）
   - frequency_penalty: 0.6（表現の繰り返し防止）

## トラブルシューティング

### 問題1: 「❌ 未設定」と表示される

**原因：** `.env.local` ファイルが正しく読み込まれていない

**解決方法：**
1. `.env.local` ファイルがプロジェクトのルートにあるか確認
2. ファイル名が `.env.local`（拡張子なし）になっているか確認
3. ファイル内容に余分なスペースや改行がないか確認
4. **サーバーを再起動**

### 問題2: 「⚠️⚠️⚠️ モックデータ」と表示される

**原因：** APIキーが読み込まれていない、またはAPIエラーが発生

**解決方法：**
1. サーバーのログで詳細なエラーを確認
2. `.env.local` ファイルを確認
3. APIキーが有効か確認
4. サーバーを再起動

### 問題3: API成功と表示されるが会話がワンパターン

**改善済み：**
- プロンプトを大幅に改善
- Temperatureを上げてより創造的に
- ペナルティ設定で繰り返しを防止

**確認：**
- サーバーを再起動して新しいコードが反映されているか確認
- 新しいディベートを開始して確認

## 重要

⚠️ **サーバーのログを必ず確認してください！**
⚠️ **「✅✅✅ OpenAI API成功！」と表示されていれば、APIは使われています**
⚠️ **「⚠️⚠️⚠️ モックデータを使用」と表示されていれば、APIは使われていません**
