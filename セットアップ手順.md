# セットアップ手順（詳細版）

## 前提条件
- Node.js（v18以上）がインストールされていること
- インストール確認：コマンドプロンプトで `node --version` と `npm --version` を実行

## 手動セットアップ手順

### ステップ1: コマンドプロンプトを開く

1. Windowsキーを押して「cmd」と入力
2. 「コマンドプロンプト」を選択して開く

### ステップ2: プロジェクトフォルダに移動

以下のコマンドを実行：
```
cd "C:\Users\yamada\Documents\アリスのお茶会"
```

### ステップ3: 依存関係をインストール

以下のコマンドを実行（数分かかります）：
```
npm install
```

**正常に完了すると、以下のようなメッセージが表示されます：**
```
added 500 packages, and audited 501 packages in 2m
```

### ステップ4: 開発サーバーを起動

以下のコマンドを実行：
```
npm run dev
```

**正常に起動すると、以下のようなメッセージが表示されます：**
```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
  - Ready in X.Xs
```

### ステップ5: ブラウザでアクセス

ブラウザを開いて以下のURLにアクセス：
```
http://localhost:3000
```

## トラブルシューティング

### エラー: 'npm' は、内部コマンドまたは外部コマンド、操作可能なプログラムまたはバッチ ファイルとして認識されていません。

**解決方法:** Node.jsがインストールされていません。以下のサイトからインストールしてください：
https://nodejs.org/

### エラー: ポート3000が使用中

**解決方法:** 別のポートで起動：
```
npm run dev -- -p 3001
```
その場合、URLは `http://localhost:3001` になります。

### エラー: 依存関係のインストールに失敗

**解決方法:** キャッシュをクリアして再インストール：
```
npm cache clean --force
npm install
```

## 補足

- サーバーを停止する場合は、コマンドプロンプトで `Ctrl+C` を押してください
- `.env.local` ファイルにAPIキーを設定している場合、OpenAI APIが使用されます
- APIキーがない場合、モックデータが使用されます
