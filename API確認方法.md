# API連携確認方法

## 問題
ディベート内容がワンパターン → モックデータが使用されている可能性

## 確認方法

### ステップ1: .env.localファイルの確認

プロジェクトフォルダに `.env.local` ファイルが存在するか確認：

```
C:\Users\yamada\Documents\アリスのお茶会\.env.local
```

**ファイルの内容：**
```
OPENAI_API_KEY=your_api_key_here
```

### ステップ2: サーバーのログを確認

サーバーを起動したコマンドプロンプトで、以下のようなログが表示されます：

**APIキーが設定されている場合：**
```
[API] APIキーの確認: 設定済み (sk-proj-vL...)
[API] OpenAI APIを呼び出し中...
[API] OpenAI API成功: ...
```

**APIキーが設定されていない場合：**
```
[API] APIキーの確認: 未設定 - モックデータを使用
[API] モックデータを使用
```

### ステップ3: ブラウザのコンソールで確認

ブラウザの開発者ツール（F12）の「Console」タブで、以下のようなログが表示されます：

```
[1/5] API呼び出し: alice
APIレスポンス: 200 OK
APIレスポンスデータ: { message: "...", scoreChange: ... }
```

## 解決方法

### 方法1: .env.localファイルを作成

1. エクスプローラーでプロジェクトフォルダを開く
2. 新しいテキストファイルを作成
3. ファイル名を `.env.local` に変更（拡張子なし）
4. 以下の内容を貼り付け：

```
OPENAI_API_KEY=your_api_key_here
```

5. 保存
6. **サーバーを再起動**（重要：環境変数はサーバー起動時に読み込まれます）

### 方法2: APIキーが正しく設定されているか確認

サーバーのログで以下を確認：
- 「設定済み」と表示されるか
- 「OpenAI API成功」と表示されるか

### 方法3: APIエラーの確認

もしエラーが表示される場合：
- `401 Unauthorized` → APIキーが無効
- `429 Too Many Requests` → API使用制限
- `500 Internal Server Error` → OpenAI側のエラー

## 確認ポイント

✅ **APIが使用されている証拠：**
- サーバーログに「OpenAI API成功」が表示される
- ディベート内容が毎回異なる
- 会話履歴に基づいた応答が返る

❌ **モックデータが使用されている証拠：**
- サーバーログに「モックデータを使用」が表示される
- ディベート内容がワンパターン
- 会話履歴が反映されない

## 重要

**環境変数を変更した場合、サーバーを再起動する必要があります！**
