# 404エラー解決方法

## 問題
- `main-app.js:1 Failed to load resource: the server responded with a status of 404 (Not Found)`
- `app-pages-internals.js:1 Failed to load resource: the server responded with a status of 404 (Not Found)`

## 原因
Next.jsのビルドファイル（`.next`フォルダ）が生成されていない、または破損しているため、必要なJavaScriptファイルが見つかりません。

## 解決方法

### 方法1: バッチファイルを使用（推奨）

1. エクスプローラーでプロジェクトフォルダを開く
2. **`修正と再起動.bat`** をダブルクリック
3. サーバーが自動的に再起動されます

### 方法2: コマンドプロンプトで手動実行

1. **実行中のサーバーを停止**（Ctrl+C）

2. **以下を実行：**

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
rmdir /s /q .next
npm run dev
```

### 方法3: 完全クリーンアップ（方法2で解決しない場合）

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"

REM 既存のサーバーを停止（Ctrl+C）

REM ビルドキャッシュを削除
rmdir /s /q .next

REM node_modulesのキャッシュを削除
if exist "node_modules\.cache" rmdir /s /q "node_modules\.cache"

REM サーバーを再起動
npm run dev
```

## 確認ポイント

サーバーが正常に起動すると、以下のようなメッセージが表示されます：

```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
- Ready in X.Xs
```

その後、`.next`フォルダが自動的に生成されます。

## よくある質問

### Q: `.next`フォルダとは何ですか？
A: Next.jsが生成するビルドファイルを格納するフォルダです。開発モードでは自動的に生成されますが、何らかの理由で破損したり、生成されなかったりすることがあります。

### Q: なぜ404エラーが発生するのですか？
A: ブラウザがNext.jsのJavaScriptファイル（`main-app.js`、`app-pages-internals.js`など）を読み込もうとしますが、`.next`フォルダが存在しない、または不完全なため、ファイルが見つからず404エラーになります。

### Q: この方法で解決しない場合は？
A: 以下を試してください：
1. ブラウザのキャッシュをクリア（Ctrl+Shift+Delete）
2. 別のブラウザで試す
3. `npm install` を再実行

## 次のステップ

上記の方法1または方法2を実行してください。その後、ブラウザでページをリロード（Ctrl+F5）して確認してください。
