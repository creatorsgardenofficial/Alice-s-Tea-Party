# エラー解決手順

## エラーの原因

```
ENOENT: no such file or directory, open 
'C:\Users\yamada\Documents\アリスのお茶会\.next\server\middleware-manifest.json'
```

**原因：** `.next` フォルダのビルドが不完全または破損しています。

## 解決方法

### 方法1: バッチファイルを使用（最も簡単）

1. **実行中のサーバーを停止**（コマンドプロンプトで `Ctrl+C`）

2. **エクスプローラーでプロジェクトフォルダを開く**

3. **`完全クリーンアップ.bat` をダブルクリック**

4. **自動的にクリーンアップとサーバー起動が行われます**

### 方法2: コマンドプロンプトで手動実行

1. **実行中のサーバーを停止**（`Ctrl+C`）

2. **以下を実行：**

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"
rmdir /s /q .next
npm run dev
```

### 方法3: 完全リセット（方法2で解決しない場合）

```cmd
cd "C:\Users\yamada\Documents\アリスのお茶会"

REM サーバーを停止（Ctrl+C）

REM .nextフォルダを削除
rmdir /s /q .next

REM キャッシュを削除
if exist "node_modules\.cache" rmdir /s /q "node_modules\.cache"

REM サーバーを再起動
npm run dev
```

## 確認ポイント

サーバーが正常に起動すると、以下のメッセージが表示されます：

```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
- Ready in X.Xs

 ✓ Ready in X.Xs
 ○ Compiling / ...
 ✓ Compiled / in XXXms
```

その後、ブラウザで表示されたURLにアクセスしてください。

## 重要

- **必ずサーバーを停止してから** `.next` フォルダを削除してください
- 削除後、サーバーを再起動すると、Next.jsが自動的にビルドファイルを再生成します
