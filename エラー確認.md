# エラー確認とトラブルシューティング

## 現在確認されている問題

ターミナルの出力を見ると、PowerShellのスクリプト実行時にエラーが発生しているようです。これはCursorのターミナルツールの問題である可能性があります。

## 診断手順

### ステップ1: 診断ツールを実行

プロジェクトフォルダで `診断.bat` をダブルクリックして実行してください。

このツールは以下を確認します：
- Node.js のインストール状況
- npm のインストール状況
- node_modules フォルダの存在
- .env.local ファイルの存在
- Next.js のビルドが正常に完了するか

### ステップ2: 手動で確認

#### 1. Node.js がインストールされているか確認

コマンドプロンプトで以下を実行：
```
node --version
npm --version
```

**結果：**
- バージョンが表示されれば → OK
- 「認識されていません」と表示されれば → Node.jsをインストールする必要があります

#### 2. 依存関係がインストールされているか確認

プロジェクトフォルダに `node_modules` フォルダが存在するか確認

**結果：**
- フォルダが存在すれば → OK
- 存在しなければ → `npm install` を実行する必要があります

#### 3. 実際のエラーメッセージを確認

コマンドプロンプトで以下を実行：
```
cd "C:\Users\yamada\Documents\アリスのお茶会"
npm install
```

**実行結果のエラーメッセージをコピーして共有してください。**

## よくあるエラーと対処法

### エラー1: 'npm' は認識されていません

**原因：** Node.jsがインストールされていない、またはPATHが設定されていない

**対処法：**
1. https://nodejs.org/ からNode.jsをインストール
2. インストール後、コマンドプロンプトを再起動
3. 再度 `node --version` で確認

### エラー2: npm ERR! コード（各種エラーコード）

**原因：** ネットワークエラー、権限エラー、ディスク容量不足など

**対処法：**
```
npm cache clean --force
npm install
```

### エラー3: ポート3000が使用中

**原因：** 既に別のプロセスがポート3000を使用している

**対処法：**
```
npm run dev -- -p 3001
```

その場合、URLは `http://localhost:3001` になります。

### エラー4: モジュールが見つからない

**原因：** 依存関係がインストールされていない、または破損している

**対処法：**
```
rmdir /s /q node_modules
npm install
```

## 次のステップ

診断ツールを実行した結果、または実際のエラーメッセージを共有していただければ、より具体的な解決策を提案できます。
